/* ============================================================
   FIREBASE CONFIG — shared by index.html and teacher.html
   ------------------------------------------------------------
   1. Go to https://console.firebase.google.com and create a
      free project (see README.md, "Firebase setup").
   2. Add a Web App and copy its firebaseConfig values here.
   3. If you leave apiKey empty, the student game runs in
      SOLO MODE (no class code, no tracking) — great for testing.
   The config values are safe to publish: security comes from
   the Firestore rules (firestore.rules), not from hiding these.
   ============================================================ */

const FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
